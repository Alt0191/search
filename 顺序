#include<cstdio>
#include<iostream>
using namespace std;;
int search(int T[], int n, int x) {
	for (int i = 0; i < n; i++)
		if (x == T[i])return i;
	return 0;
}

int main() {
	int T[1000], n, x;
	cin >> n;
	for (int i = 0; i < n; i++)cin >> T[i];
	cin >> x;
	cout << search(T, n, x);
}
